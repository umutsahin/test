ingress:
  enabled: true
  className: nginx
  hosts:
    - paths:
        - path: /
          pathType: Prefix
gitea:
  config:
    server:
      DOMAIN: 0.0.0.0
      ROOT_URL: http://0.0.0.0:32000/
  admin:
    username: optiva
    password: password
    email: "optiva@optiva.com"
service:
  http:
    type: NodePort
    nodePort: 32000
